## Mutations
type Mutation {
  ## Barn
  adminDeleteBarn(input: AdminDeleteBarnInput): AdminBarnResult!

  ## Horse

  ## Marketplace

  ## User

  ## Org
}


## Admin delete barn
input AdminDeleteBarnInput {
  barnId: ID!
}

type AdminBarnResult {
  result: MsResult!
  error: AdminDeleteBarnError
}

enum AdminBarnError {
  NOT_AUTHORIZED
  INVALID_ADMIN_KEY
  INVALID_BARN_ID
  NONEXISTENT_BARN_ID
  SERVICE
}